Requirements

    Java 11 or higher (supports modern Java features)
    Bash shell (for running run.sh on Linux/Mac; use equivalent commands for Windows)

Usage
Running the Interpreter

    Ensure Java is installed:

java --version

Make the run.sh script executable:

chmod +x run.sh

Run the Interpreter:

./run.sh <filepath> [--trace]

    <filepath>: Path to the .fp file containing the logic program.
    [--trace]: Optional argument to enable trace mode for debugging.